# Environment variables
.env
.env.*
!.env.example

# Model weights — fetched via tools/fetch-models.sh, never committed
*.onnx
*.onnx.json
*.bin

# Node
node_modules/
.turbo/
apps/backend/dist/
apps/backend/*.tsbuildinfo
packages/*/dist/
packages/*/*.tsbuildinfo
.eslintcache

# Local Claude config / vendored skills (per-developer, not shared)
.agents/
.claude/skills/
.claude/settings.local.json
skills-lock.json

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDE
.idea/
.vscode/
*.swp
*.swo

# Logs
*.log

# Per-session transcripts written by the file transcript repository
transcripts/

# STT debug WAV dumps (DEBUG_STT_WAV=1)
debug-audio/
